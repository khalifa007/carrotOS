# Public repo is user-facing only (flash README + Releases).
# All build harness + dev artifacts stay local.

# Build / release outputs
release/
out/

# Build harness scripts
setup_lineage.sh
setup_cipheros.sh
slim_lineage.sh
verify_lineage_state.sh
catch_fastboot.sh

# Dev / internal docs
CLAUDE.md
claude.md
build.md
.claude/

# Build artifacts shipped inside the release zip
vbmeta.img
vbmeta.img.*
carrotos.png
vendor_fix/

# Launcher prebuilt
app-debug.apk

# Backups + screen captures
.slim_backup/
screen-*.png
